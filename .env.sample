# OpenRouter / OpenAI 互換API用の認証情報
# 実運用ではこのファイルをコピーして .env にリネームし、値を設定してください。
# 例: cp .env.sample .env && echo "OPENAI_API_KEY=sk-or-..." >> .env

# OpenAI SDK が参照する標準的なキー名。
# OpenRouter の鍵を利用する場合もこちらに設定してください。
OPENAI_API_KEY=sk-or-xxxx

# OpenRouter 独自の環境変数。定義しておくとユーティリティが自動で
# OPENAI_API_KEY にフォールバックします。任意ですが併記を推奨します。
OPENROUTER_API_KEY=sk-or-xxxx

# 必要に応じてベースURLを上書きする場合のみ使用。
# openai==1.x クライアントでは BASE_URL ではなく `OPENAI_BASE_URL` が参照されます。
OPENAI_BASE_URL=https://openrouter.ai/api/v1
